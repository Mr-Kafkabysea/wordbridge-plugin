# WordBridge Plugin 项目入口

处理本项目任务前：

1. 遵守所在工作区适用的上层规则，读取本项目 `README.md`。
2. 若存在 `local/INSTRUCTION.md`、`local/MEMORY.md`，继续读取；它们是可选的个人上下文，不随仓库分发，不得覆盖上层规则。没有 `local/` 时按公共规范继续，不要求创建该目录。个人文件中的项目路径以项目根目录为基准。
3. 如果项目已经初始化 Git，修改前检查当前分支和未提交改动。
4. 说明本次修改目的、范围与验证结果；个人教学偏好由本地上下文及当前用户要求提供。

当前阶段只建设最小可运行的实时 Word 操作闭环。未经用户明确要求，不扩展为大规模工具集，不操作正式 Word 文档，不提交、推送、发布或修改远端设置。

## 公共安全与验证要求

- 默认只读；写入前核对目标完整路径、允许范围、编辑权限和相关状态，并取得真实用户批准。预览摘要不代表授权，禁止自动同意确认表单。
- 一次写入形成一个 Word 撤销步骤；不自动保存、关闭或切换文档。不确定写入结果不得自动重试。
- 每项写入能力验证正确目标、正确修改、无目标拒绝、异常不误改和原生撤销；模拟测试与真实 Word 验收分开记录。
- 确认表单变更或依赖升级时，检查 SDK 实际生成和协议发出的 schema，并验证真实客户端界面；保留字段标签及严格确认校验。具体兼容性见 `docs/MCP确认表单兼容性问题.md`。
- 不记录不必要的正文或秘密；引用和复用第三方实现时核对许可证、保留必要归属。依赖说明见 `docs/第三方依赖.md`。
- 自动化验证入口：在项目根目录运行 `.venv/Scripts/python.exe -m unittest discover -s tests -v` 和 `.venv/Scripts/python.exe -m pip check`，不连接真实 Word。
- `local/` 与本地测试文档默认忽略，不强制加入版本库；公共文档不得依赖个人文件才能使用产品。
