# 更新日志

版本标签对应可复现的源码快照；`-dev` 表示开发验证版，不代表稳定版；是否已有测试包以对应 Release 为准。

## 0.3.0 — 2026-09-11（首个公开 MVP）

- 发布 Windows 桌面 Word 最小操作闭环：连接、当前文档、选区读取、文末追加预览与人工确认写入。
- 统一安装升级入口、同版本无需更新，以及升级过程内的失败恢复。
- 修复 Python 已安装但 py Launcher 不可用时的误判；新增 python/python3 回退及 -PythonPath。
- 采用 MIT 许可证，发布包附带 LICENSE 和 SHA-256 清单。
- 汇总真实预发布验收结果；0.3.0 自身的用户侧升级与跨机器验证仍待完成。

## 0.3.0-dev — 2026-09-11（预发布）

- 将 install.ps1 扩展为统一入口：首次安装、较新版本升级、同版本跳过；显示版本差异并拒绝降级。
- 保留先前已完成的首次安装能力、独立环境、白名单打包和 MCP/Skill 同包安装。
- 新运行环境按版本隔离，兼容旧安装器固定目录；检查依赖、MCP 实际握手版本、Codex 注册和缓存。
- 升级失败时尝试恢复旧包及客户端注册；恢复不完整或异常中断时保留事务记录并阻止继续更新。
- 保留旧环境、旧包和失败文件，不自动清理，不操作 Word，不自动下载新版。
- 安装器临时目录测试及真实已安装版本的只读预检通过；实际 MCP 握手通过，真实客户端升级仍待验收。

### 此前未发布的打包与首次安装工作

- 完成首次安装入口，用户已确认本机 0.2.2-dev 安装、连接和追加使用跑通。
- 仓库、目录及插件标识改为 wordbridge-plugin，产品名称为 WordBridge Plugin。
- 建立 Codex 插件清单、MCP 配置、配套 Skill 和白名单打包脚本。

## 0.2.2-dev — 2026-09-11

### 首次连接提示与后台状态

- 每个 MCP 服务实例首次有效工具调用时自动弹窗，只读检查 Word 连接；成功显示已就绪并在 3 秒后关闭，失败保留提示。
- 仅启动服务、完成握手或获取工具列表不会弹窗；同一实例后续调用不重复弹出。
- 保留手动诊断窗口，展示服务心跳、协议状态、最近收到的工具及 Word 检查时间；超过 15 秒无心跳不再宣称在线。
- 后台快照按实例分开，存放于被忽略的 `local/runtime/connections/`，不记录正文、文档路径或工具参数。

### 使用变化与边界

- 更新后重新加载 MCP 服务以启用首次提示；Tk 桌面窗口使用 Python 自带 tkinter，不新增第三方依赖。
- 首次自动检查只读取连接信息，不代表当前文档可编辑，不代替写入确认；后续安全检查、预览和撤销规则保持不变。
- 连接成功只是当时的检查结果。多个独立服务实例可能各弹一次；旧记录保留，暂不识别模型或展示完整调用结果历史。
- 弹窗或状态文件失败不应阻断原工具调用；失败提示不自动重试用户操作。

### 验证

- 73 项模拟测试及依赖检查通过；覆盖首次调用、重复调用、弹窗失败、Word 不可访问与恢复、隐私字段和过期状态。
- 实际 stdio 协议通信和退出记录已检查；模拟状态下真实 Tk 窗口成功自动关闭、失败保留通过。
- 用户反馈当前弹窗效果可用；尚不能据此宣称跨机器、跨模型或复杂 Word 场景全部验收通过。

## 0.2.1-dev — 2026-09-11

### WordBridge Skill

- 新增独立的 `skills/wordbridge/SKILL.md`，明确实时 Word 请求的工具发现、任务路由及失败处理。
- 用户要求 WordBridge 时优先查找其 MCP 工具；不擅自改走电脑控制，不把其他工具的错误误报为 WordBridge 失败。
- 保留目标复核、追加预览、真实人工确认，以及写入结果不确定时停止重试的要求。
- Skill 需由客户端单独安装加载，不改变 MCP 的连接范围；未打包为插件。

### 验证与限制

- 用户安装后反馈当前场景可以使用；跨模型首次调用的一致性仍待系统验收。
- 人工核对工具参数和安全边界。Skill 自动格式校验因校验环境缺少 YAML 依赖未完成。
- MCP 功能沿用 0.2.0-dev，不包含状态窗口或首次调用弹窗；本版不新增运行依赖。

## 0.2.0-dev — 2026-09-10

### 修复与改进

- `get_active_document` 和 `get_selection` 的 `expected_document` 改为可选。省略时从已连接 Word 实例识别当前文档，不再要求用户先提供路径；传入时仍严格核对目标。
- 移除读取、预览及追加操作的 `test-documents` 目录白名单，支持其他目录中已存在、已保存的本地 `.docx`。
- 统一读写路径有效性检查；同步 MCP 工具说明、确认提示和使用文档。
- 增加无路径 MCP 调用、当前文档识别、选区身份复核、空选区及非法文件输入的回归覆盖。

### 兼容性与升级

- 文件输入错误状态由 `invalid_test_file` 更名为 `invalid_document_file`；依赖此状态的调用方需同步更新。
- 目录白名单不再提供隔离。调用者应核对返回路径，写入前确认目标和内容；建议先用文档副本验收。
- 更新源码后需重新加载 MCP 服务和工具列表，正在运行的旧进程不会自动加载新接口。
- 命令行读取脚本仍要求 `--expected-document`；本次省略路径的使用入口是 MCP 工具。

### 保留的安全边界

- 只连接已有 Word，不自动启动、打开、切换、保存或关闭文档。
- 读取选区前再次核对所属文档；只有光标时返回 `empty_selection`，不自动读取所在段落。
- 追加仍需显式目标、预览摘要和真实用户确认；保留状态复核、单步撤销及结果不确定时禁止自动重试。
- 未保存文档、非 `.docx` 和网页 URL 不受支持；写入仍拒绝只读、编辑保护、修订、内容控件等不支持的场景。

### 验证与已知限制

- 64 项模拟测试通过；`pip check` 通过。模拟测试不连接真实 Word。
- 本版无路径读取与目录范围扩大尚未完成真实 Word / 客户端验收，不能沿用旧版验收结果宣称通过。
- 多个独立 Word 实例的目标选择、复杂格式、主动取消确认的真实界面及另一台电脑的独立安装仍待验收。
- 项目许可证仍未选定；本版未新增依赖。

## 0.1.0-dev — 2026-09-10

初始 GitHub 源码基线：`31b3b36`（未单独建立版本标签）。

- 提供 Word 状态、当前文档、选区读取、追加预览及确认追加共 5 个 MCP 工具。
- 初版只允许项目测试目录中的 `.docx`，读取接口要求目标路径。
- 修复确认表单顶层 `title` 导致的客户端解析失败，保留字段标签和严格确认校验。
- 56 项模拟测试通过；本机普通正文真实 MCP 追加及用户原生撤销确认通过，复杂场景未验收。
- 公共代码及说明进入仓库，个人上下文、环境和测试文档不随仓库分发。
